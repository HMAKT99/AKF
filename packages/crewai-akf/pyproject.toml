[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "crewai-akf"
version = "0.1.0"
description = "CrewAI integration for Agent Knowledge Format (AKF)"
requires-python = ">=3.10"
dependencies = [
    "akf>=1.0.0",
    "crewai>=0.1.0",
]

[project.optional-dependencies]
dev = ["pytest>=7.0"]
